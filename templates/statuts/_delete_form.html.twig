<form method="post" action="{{ path('app_statuts_delete', {'id': statut.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce statut?');" class="m-0">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut.id) }}">
    <button class="btn"><i class="fa-solid fa-circle-xmark fa-2xl" style="color: #ff0000;"></i></button>
</form>
