{% extends 'base.html.twig' %}

{% block title %}Statuts{% endblock %}

{% block body %}
    <h1>Statuts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ statut.type }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ statut.couleur }}</td>
            </tr>
            <tr>
                <th>Definitif</th>
                <td>{{ statut.definitif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statuts_index') }}">back to list</a>

    <a href="{{ path('app_statuts_edit', {'id': statut.id}) }}">edit</a>

    {{ include('statuts/_delete_form.html.twig') }}
{% endblock %}
