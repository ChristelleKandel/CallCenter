{% extends 'base.html.twig' %}

{% block title %}create{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
        <a class="btn btn-lg btn-bleu my-2 mx-auto" href="{{ path('app_missions_index') }}"><i class="fa-solid fa-arrow-left"></i> Retour</a>
    </div>
    <h1 class="text-center">Nouvelle mission</h1>

    {{ include('_partials/_form.html.twig') }}
    <div class="row col-md-6 mx-auto">
        <div class="d-flex justify-content-center col-md-4 mx-auto d-grid gap-2">
            <a href="{{ path('app_statuts_index') }}" class="btn btn-lg btn-bleu m-100" target="_blank">
                <i class="fa-solid fa-magnifying-glass"></i> 
                Les statuts
            </a>
        </div>
        <div class="d-flex justify-content-center col-md-4 mx-auto d-grid gap-2">
            <a href="{{ path('app_prerempli_index') }}" class="btn btn-lg btn-bleu" target="_blank"><i class="fa-solid fa-magnifying-glass"></i> Les pré-remplis</a>
        </div>
        <div class="d-flex justify-content-center col-md-4 mx-auto d-grid gap-2">
            <a href="{{ path('app_champs_index') }}" class="btn btn-lg btn-bleu" target="_blank"><i class="fa-solid fa-magnifying-glass"></i> Les champs</a>
        </div>
    </div>
{% endblock %}
