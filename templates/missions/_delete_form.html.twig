<form method="post" action="{{ path('app_missions_delete', {'id': mission.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette mission?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
    <button class="btn btn-lg btn-violet d-grid gap-2 col-6 mx-auto">Supprimer la mission</button>
</form>
